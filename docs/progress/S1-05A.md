# S1-05A progress record

The goal: [S1-05A](../alignment/2026-09-25/S1-05A_GOAL_SPEC.md) — make the shared room a real Sophia conversation with one durable dsh task. This record keeps four things apart for every acceptance case: what source exists, what tests ran, what hosted evidence exists and what humans accepted. A status changes only with the evidence named beside it. The frozen `docs/pack/delivery/planning.json` is not edited.

**Verdict now:** candidate ready for review (attempt 1): checkpoints A–D built and passing locally and in CI. Not accepted: no live model, media or hosted run has happened, and no operator receipt exists yet. See [the handoff](../handoffs/S1-05A-attempt-1.md).

| Case | Source | Tests (this repo) | Hosted evidence | Human acceptance |
|---|---|---|---|---|
| A01 baseline and hosted tuple; no stale auto-deploy | [checkpoint A](../evidence/S1-05A/checkpoint-A.md) records the source graph | baseline `pnpm check` and `pnpm test:db` pass on `29a570c3` | **met**: the baseline is observed; the stale auto-deploy was **confirmed on**, then turned **off** in CX-0013; the release OP-0002 put every process at `ce8bb8f` (CX-0043, note 1) (Codex [CX-0004](https://github.com/davidelaverga/Sophia/issues/14#issuecomment-5835343541), [CX-0005](https://github.com/davidelaverga/Sophia/issues/14#issuecomment-5835349474); see note 1 below) | — |
| A02 real runtime service ↔ pinned dsh; foreign project/lease/unit refused | built ([checkpoint B](../evidence/S1-05A/checkpoint-B.md)) | crossing test 5/5 and DB tests pass locally (mock model) | — | — |
| A03 discussion + one idempotent brief admission through the API | built | API and DB tests; crossing test | — | — |
| A04 real model produces a source-backed brief | built (capture, model identity, usage) | mock route only | **pending**: owner allowance and `OPENAI_API_KEY` on the execution host (`scripts/runtime-host.mjs`) | — |
| A05 two humans, real Gemini, one shared Sophia | built ([checkpoint C](../evidence/S1-05A/checkpoint-C.md): media bridge, exchange, room UI) | fakes only for Google; real LiveKit server for the RTC side | **pending**: owner allowance, `GEMINI_API_KEY` and LiveKit credentials on an authorized host | — |
| A06 voice request starts the brief once; result surfaced once | built | real API + DB with fake Google: admitted once for the holder, replay admits nothing; notice once when idle | pending (live run) | — |
| A07 steer delivered and incorporated at the native boundary | runtime half built; voice half open | crossing test (mock model) | — | — |
| A08 Hold/Stop and late replies cannot restart or publish | built | DB tests (with mutation check) and crossing test | — | — |
| A09 Stop Speaking / End exchange leave work intact | built | bridge session tests (fakes) and the API crossing: output cleared, work untouched | pending (live run) | — |
| A10 floor transfer keeps the utterance actor | built | exchange model, session and DB tests; stale holder events refused by compare-and-set | — | — |
| A11 selected screen only; Stop Looking survives reconnect | built | session tests; real-LiveKit test (an unchosen screen never arrives; frames stop after Stop Looking) | pending (live run: a screen actually discussed) | — |
| A12 guest admission pauses project-aware AI; removal reconciled | built (quiescence in [C](../evidence/S1-05A/checkpoint-C.md); durable removal in [D](../evidence/S1-05A/checkpoint-D.md)) | DB and API crossing: token only after the bridge's acknowledgement, 503 without it; a failed removal stays pending and the worker settles it on evidence; the real LiveKit server disconnects a real guest | pending | — |
| A13 viewer converses but cannot control work | built | API crossing: a viewer holds the floor and reads status; their brief is refused | — | — |
| A14 Google reconnect and runtime restart keep work identity | built (runtime half in B, Google half in C) | crossing test (restart); session tests (GoAway, resumption, no stale answers) | pending (live forced reconnect) | — |
| A15 light and status follow actual media/work state | built (`sophia-view.ts` projection, dock controls, observation indicator) | projection unit tests; browser run with the real room path and the **rehearsal** bridge (no Google) | pending (live run on real devices) | — |

**Note 1 (A01 hosted evidence).** Codex's reconciled read-only observations (CX-0004 supersedes CX-0002, CX-0005 supersedes CX-0003), made in Davide's signed-in provider projects:

- **API service.** Render `sophia-next-api` (`srv-daqrn08473hc73flhi8g`) serves deploy `dep-dar15tk9v7es7394a1q0` from `29a570c3`. **Auto-Deploy is On Commit** for branch `deploy/render-vercel`, which is `ff22a249`, older than the deployed code. A commit on that branch would deploy it.
- **Schema.** The hosted ledger holds exactly 0001–0011, and every filename and checksum matches `29a570c3`. 0012 onward are absent. Ledger fingerprint: `ee2f78839c2d14fb04a9376699820c7f5b04cb6bf82eb847991e174fff6ce2cf`.
- **Roles.** `sophia_api_app` can log in, without BYPASSRLS or SUPERUSER. `sophia_worker` exists only as a NOLOGIN role, so no worker login exists yet. The migration connection's TLS and pooler mode are unverified.
- **API settings (names only).**
  - `LIVEKIT_URL` is populated, but `LIVEKIT_API_KEY` and `LIVEKIT_API_SECRET` are blank.
  - `STUDIO_ORIGINS` is blank. The Supabase JWT settings are populated.
  - The invitation and mail settings are absent.
- **Two hazards for the next start of the API**, whichever commit it runs:
  - Both `29a570c3` and the candidate treat the LiveKit key and secret as required once `LIVEKIT_URL` is set, so with those blanks the API exits at startup.
  - A blank `STUDIO_ORIGINS` sends no CORS headers, so Studio in the browser cannot call it.
- **Auth.** Public signup is **enabled** and anonymous sign-ins are **disabled**. Studio's guest path calls `signInAnonymously`, so guests would see "Guest access is not turned on for this Sophia yet". The Site URL and redirect are `sophia-studio.vercel.app`, plus localhost.
- **Studio.** Vercel deployment `dpl_AFUZSgyPTD9zRnQEoGazBf6o3Crp` has no Git link, so its source commit is unknown.
- **Long-running processes.** No worker, media bridge or execution host exists on Render, and no VM was found.
- **LiveKit.** Project `p_40misr7kwz5` (EU, Frankfurt) has agent observability on and **PII redaction off**. It had no sessions in the last 24 h, and trace retention is unknown.
- **Diagnostic.** Not run on hosted services. Codex has not yet reviewed the hardened sanitizer (`833221c`).

**Update, 2026-09-25 18:06 UTC (CX-0006 to CX-0013).** Things changed outside the operator:
- **API.** It now serves `dep-darb9e6gekts738ucd0g` from `878aa9e6` on branch `studio/qol`. **Auto-Deploy is Off** (OP-0004 revision 2, verified in CX-0013), so the stale-auto-deploy half of A01 is resolved.
- **API settings (CX-0009).** The LiveKit key and secret are nonblank, `STUDIO_ORIGINS` is the Studio origin and `HOST` is `0.0.0.0`, so both hazards above are cleared. `RESEND_API_KEY` is set and `INVITE_FROM` is absent; since `878aa9e` that turns invitation email off instead of stopping the API.
- **Studio (CX-0010).** Production is `JDM4Ng5TjbcyzQraiHtNFhmDurvP`, deployed with `vercel deploy`; its commit is not shown.
- **Schema.** Still 0001–0011 (CX-0008).
- **Diagnostic.** Codex reviewed the sanitizer (CX-0007); its findings are fixed at `36cd1f2`.

The release request is now revision 3, [CC-0010](../coordination/S1-05A/S1-05A-CC-0010.md), corrected by [CC-0011](../coordination/S1-05A/S1-05A-CC-0011.md). The API stays free, and the other three processes become Render Starter background workers. Codex's read-only review CX-0014 (18:58 UTC) re-observed:
- the ledger at 0001–0011, still fingerprint `ee2f7883…`;
- the API unchanged, holding `GEMINI_API_KEY` and `OPENAI_API_KEY`;
- Studio Production built from `b3da19e` or later.

Execution waits on Davide's explicit approval of E1–E10, a migration-owner access path, and the project id and admin email.

**Release, 2026-09-26 (OP-0002, CX-0021 to CX-0043).** Codex executed E1–E10 under Davide's approvals: E1–E7 in the operator task; E8–E10 at exact `ce8bb8f` as three Starter services plus a 1 GB disk, under USD 30/month with a seven-day expiry. Each step was verified on its own:

| Step | Result |
|---|---|
| E1 | Schema at 0001–0014, 0 pending, checksums matching |
| E2 | Restricted `sophia_worker_app` login |
| E3 | Runtime registered |
| E4–E5 | Bridge capability; the API's hash matched by equality |
| E6 | API `dep-daruot17lnhs73eu62ig` at `ce8bb8f`, still on the free instance |
| E7 | Studio `dpl_7gknKbF9DfkScVLDTnZVTWa45byU`, byte-identical to the `ce8bb8f` build |
| E8 | Worker `srv-darv7snpn0mc73e6c240` |
| E9 | Bridge `srv-darvcigu01pc73e24o4g` |
| E10 | Runtime host `srv-darvmse0tbcc73d8kh3g`; artifacts reproduced on Linux, `live:true`, ready |

Smoke checks observed: S1 (`/health`, `/ready`), S3 (worker banners), S4 (runtime ready), S5 (`/v1/media/assignments` 200). Rollback targets are kept: API `dep-darb9e6gekts738ucd0g` and Studio `JDM4Ng5TjbcyzQraiHtNFhmDurvP`. In the owner's first live test Sophia answered by voice. CX-0044 reported a spurious `holder_left` pause when a bridge joined (CC-0017). CX-0045 and CX-0046 reported longer replies skipping: a 2 s drop-oldest output backlog (CC-0018). Both fixes were code-reviewed before any redeploy, and the review's findings are fixed at `f31cacd` (CC-0020). Codex's end-to-end review of `fa97a1a` then found three more, fixed at `44e68c2` (CC-0021), one by migration 0015. Davide approved applying 0015 on its own (OP-0007, CC-0022). His next Codex review round revised it before it ran, to fence a guest token for its full ten minutes (CC-0023 held it; CC-0024 re-issued it at `072c789`, and Davide confirmed that revision in CC-0025). Codex's preflight (CX-0047, nothing applied) then found the fence started at the token request rather than the mint; 0015 now stamps it again just before the mint (`e4d2e13`, CC-0026), which Davide approved by name (CC-0027). Codex held it again (CX-0048): with the live API, a database-only release cannot bound a late mint. Davide chose to release the database and the API together: OP-0007 revision 4 (CC-0028) applies 0015 and 0016 (the four fixes from Codex's review of `aba31d5`), then deploys the API at `2d59884`. Davide approved the exact commit and hashes directly with Codex.

**Follow-up release, 2026-09-26 (OP-0007 revision 4, CX-0049 to CX-0052).** Codex executed and verified it at 21:19–21:23 UTC:

| Step | Result |
|---|---|
| M1 | 0015 (`07eff401…`) and 0016 (`3c83a624…`) applied; the ledger is 0001–0016, 0 pending, with matching checksums; `guest_joining`, `guest_token_minting` and `capture_completed_turns` present; the old API still `/ready` 200 |
| M2 | API `dep-das3ed7pn0mc73eleaj0` at `2d59884`; `/health` and `/ready` 200; Auto-Deploy Off |

The worker, bridge, runtime host and Studio stay at `ce8bb8f`, which runs unchanged on 0015 and 0016. The API's rollback target is now `dep-daruot17lnhs73eu62ig` (`ce8bb8f`). The in-app guest-join refusal was not tested. The bridge and Studio redeploy (OP-0006) waits for Davide's review round.

Original S1-03 and S1-05 criteria that S1-05A does not cover stay open in their own goals (image/prototype admission in S1-06/S1-07/S1-12; full S1-05 acceptance needs its live episode).
