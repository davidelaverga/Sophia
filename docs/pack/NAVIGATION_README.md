# Part 1 — navigation repair

This is a navigation-only repair of `Sophia_Implementation_Pack_v0.3_Part1_2026-09-24`.
The product plan, model choices, goal content, dependencies and implementation status are unchanged.
All 57 files from the original ZIP are preserved byte-for-byte.

## Recommended way to read

Open `OPEN_GOALS.html` in your browser. It embeds all 14 Sprint 1 goal briefs and every supporting file from the original pack. Its document links are internal anchors, so it does not need a server, sibling files, external scripts or an internet connection to navigate the pack. External research websites remain external links.

The sidebar finds goals and documents by title or filename. The browser Back and Forward buttons follow your navigation. On a phone, use Contents to reveal the sidebar. Mermaid diagrams remain as source text; this repair does not change or regenerate diagrams.

## Markdown and coding-agent use

Extract the entire ZIP before using the Markdown files. Open `delivery/GOAL_INDEX.md` in a Markdown-capable editor, keeping `delivery/goals/`, `architecture/`, `sources/` and the other folders in place. The original relative links are correct in that folder layout.

Opening only the index as a standalone attachment loses access to its relative folder targets. The separately supplied Chat Link Fix copies use sandbox links for this conversation; do not use those copies as repository documentation.

## Validation

The original 15 links in the Goal Index (planning.json plus 14 goal briefs) were resolved. All 113 local Markdown links in the original pack were checked against actual archive files. All embedded-document reader links were checked for matching anchors. All sandbox-file links in the chat navigation copies were checked against files present in the active runtime. Browser test results are recorded in `evidence/navigation-validation.json`.

External research links and the ChatGPT attachment viewer were not remotely tested by this repair.
