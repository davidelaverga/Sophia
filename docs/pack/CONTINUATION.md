# Continuation checkpoint — execute the connected implementation

## Active map

Use this cumulative v0.4 Part 2 pack. It retains the user’s three-release direction and Part 1’s dsh/Google/media choices. It adds the selected native binding matrix, database/API candidate layer, actual Studio crosswalk, renderer extraction unit and all 24 individual goal briefs. The prior v0.3 ZIP remains historical, not a second active instruction set.

## Next execution increments

**First: S1-02 data/API and S1-09 native resource work in parallel.** Run the SQL on a disposable PostgreSQL/Supabase instance and add real concurrency/RLS tests. Implement the API command/snapshot/replay slice. On the engineer side, execute the owner device-flow → dormant native bundle → scoped MCP → pre-launch model PATCH → host worktree → readiness sequence on all three resources. Record actual installed versions, model settings and permission coverage. These are selected paths to implement, not framework-selection experiments.

**Second: connect dsh control, peer dialogue and supervision.** Implement the application/runtime bridge against the public Agent API; preserve native dsh inbox semantics. Use real external peer question/reply and five-minute/manual lead reviews. Exercise Stop racing a pending send, an uncertain response, a sleeping host and a late permission. Reference fixtures establish syntax/state rules only; native proof requires actual sessions.

**Third: Studio/voice/prototype/renderer integration.** Transfer the actual reference semantics into the named React components, preserve local drafts and shared focus distinction, run two-person Gemini Live through the retained media design, generate both image routes, produce editable UI source and return the complete source-linked app preview. Extract/adapt the three verified renderer kernels and create useful report/deck outputs.

## Remaining documentation and implementation gaps, precisely

- Specialized SQL/domain handlers beyond the supplied foundation: membership changes, source sharing/Forget cascades, native delivery settlement, decision acceptance, validator publication and budget reservations. Follow architecture 12; no generic unrestricted write API.
- Actual dependency/browser/runtime image lock and digests. The desired deployment map is supplied; no OCI image, cloud service or vendor account was provisioned here.
- Exact installed native custom-bundle/MCP behavior and catalogue support. Route/source binding is audited, live compatibility is not.
- Real media/provider/source-revocation tests. Part 1’s model/API choices remain the configured targets; inherited documentation is not a new account-availability claim.
- Full production React implementation, runtime response validators and bounded SSE cache/controller. The reference reducers and client are selected examples, not the whole app.
- Actual renderer extraction/build/sandbox tests. The helper verifies pinned Git blobs; it was not run against a mounted source clone.
- Impeccable installed skill closure and any selected connector-specific implementation audit. These stay bounded work within the existing goals, not new root dependencies.

S2/S3 now have concrete briefs and session sequences. Their later detailed provider/connector actions must be refreshed when implemented; do not claim a September source pin guarantees future vendor behavior.

## What is already tested

See evidence/ for exact logs: local reference TypeScript and API client compilation/tests, API type generation, JSON schema examples, source-anchor/hash consistency, local documentation links and the goal dependency graph. No live dsh boot, native three-resource episode, Google/OpenAI request, SQL/RLS execution or deployment was tested.

## Handoff discipline

Continue with the same goal IDs and latest accepted decisions. Record the actual base/head, source pins, executed checks and unresolved external effects after each session. Keep original user intent and disagreement visible; do not require mission scoring or a forced pivot to demonstrate learning. A stopped goal is not automatically resumed because a new coding chat starts.
