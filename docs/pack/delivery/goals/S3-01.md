# S3-01 — Authorized multi-week project continuity

**Release:** Sprint 3 · **Owner:** Davide · **Implementation:** not_started

## User result

Continue dependent goals, reconcile unavailable hosts and uncertain effects, and preserve the mandate across idle periods and runtime restarts.

## Dependencies and reading

Dependencies: S2-01, S2-02, S2-03, S2-04, S2-05, S2-06. Read [agent map](../../AGENTS.md), [11_OMNIGENT_BINDINGS](../../architecture/11_OMNIGENT_BINDINGS.md), [12_DATA_AND_API_BINDINGS](../../architecture/12_DATA_AND_API_BINDINGS.md), [09_MEMORY_AND_CONTEXT](../../architecture/09_MEMORY_AND_CONTEXT.md). Source IDs: DSH-16, OM-03, OM-05, OM-09. Read the exact registered revision before coding the seam.

## Code destinations

- `apps/api/src/services/continuity.ts`
- `packages/dsh-bundle/src/lead/continuity.ts`
- `tests/continuity/`

## Implementation sessions

### S3-01.1 — bounded work session

Drive successive dependency-ready goals from accepted project state rather than whichever agent session happens to remain open.

Finish with the actual source change, relevant checks, unresolved facts and the next session’s starting point. No session completion by itself closes this goal.

### S3-01.2 — bounded work session

Maintain one continuation owner for each native/dsh route and a bounded five-minute/event/manual review policy with wait backoff.

Finish with the actual source change, relevant checks, unresolved facts and the next session’s starting point. No session completion by itself closes this goal.

### S3-01.3 — bounded work session

Reconcile quota, host loss, credential expiry and uncertain effects before proposing reassignment; preserve prior attempts, observations and rejected approaches.

Finish with the actual source change, relevant checks, unresolved facts and the next session’s starting point. No session completion by itself closes this goal.

### S3-01.4 — bounded work session

Compile a fresh alignment brief when context or approach becomes stale; do not erase completed source or adopt old model summaries as new authority.

Finish with the actual source change, relevant checks, unresolved facts and the next session’s starting point. No session completion by itself closes this goal.

### S3-01.5 — bounded work session

Run a multi-day project with people leaving/returning, a host unavailable and an explicit revised priority; verify the actual next authorized goal.

Finish with the actual source change, relevant checks, unresolved facts and the next session’s starting point. No session completion by itself closes this goal.

## Acceptance evidence

- The project advances across sessions without repeated human reconstruction of its accepted brief.
- A real dependency waits for acceptance rather than a native done claim.
- Returning users understand what changed, what remains uncertain and the next choice.

## Adverse checks

- A second supervisor cannot revive or double-dispatch the same goal.
- A denied permission cannot be routed around through another account.
- An expired refresh response does not trigger an old-token retry storm.

## Handoff and acceptance

Use the [session handoff](../../templates/SESSION_HANDOFF.md). Retain the accepted source and prior usable output on failure. Record any operation whose external effect remains unknown; never present source edits alone as a live integration. Human acceptance or the current explicit acceptance policy closes the goal.
