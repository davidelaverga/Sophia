# S2-03 — Scenarios and executable application review

**Release:** Sprint 2 · **Owner:** Davide · **Implementation:** not_started

## User result

Add Storybook/MSW scenario fixtures and Playwright assertions/traces; distinguish simulation, recorded replay and live application evidence.

## Dependencies and reading

Dependencies: S1-14. Read [agent map](../../AGENTS.md), [13_FRONTEND_BINDINGS](../../architecture/13_FRONTEND_BINDINGS.md), [14_RENDERER_EXTRACTION](../../architecture/14_RENDERER_EXTRACTION.md). Source IDs: DSH-17, TEST-01, UI-01. Read the exact registered revision before coding the seam.

## Code destinations

- `apps/studio/.storybook/`
- `packages/scenarios/`
- `tests/preview-e2e/`

## Implementation sessions

### S2-03.1 — bounded work session

Promote S1 state fixtures into versioned Storybook scenarios for the chosen React component stack, with MSW-owned network fixtures.

Finish with the actual source change, relevant checks, unresolved facts and the next session’s starting point. No session completion by itself closes this goal.

### S2-03.2 — bounded work session

Bind scenario source and mocked service contracts to the same candidate family as the actual application. Show simulated versus integrated state prominently.

Finish with the actual source change, relevant checks, unresolved facts and the next session’s starting point. No session completion by itself closes this goal.

### S2-03.3 — bounded work session

Run Playwright flows against the complete preview: assertions, console/network evidence, trace and captured source/deployment identity.

Finish with the actual source change, relevant checks, unresolved facts and the next session’s starting point. No session completion by itself closes this goal.

### S2-03.4 — bounded work session

Use selected Google video review as timestamped observations; turn useful discoveries into executable regression tests.

Finish with the actual source change, relevant checks, unresolved facts and the next session’s starting point. No session completion by itself closes this goal.

### S2-03.5 — bounded work session

Keep dsh model-stream replay for agent-control tests, UI scenarios for simulated states and real application tests for actual integration. Do not merge their evidence labels.

Finish with the actual source change, relevant checks, unresolved facts and the next session’s starting point. No session completion by itself closes this goal.

## Acceptance evidence

- Loading, empty, failure and success scenarios run and can be inspected from Sophia.
- A real application flow runs with assertions and a source-bound trace.
- One visual observation becomes a failing then passing executable check.

## Adverse checks

- A replayed model stream is not reported as a fresh live model test.
- A simulated payment/database response cannot pass an integrated acceptance criterion.

## Handoff and acceptance

Use the [session handoff](../../templates/SESSION_HANDOFF.md). Retain the accepted source and prior usable output on failure. Record any operation whose external effect remains unknown; never present source edits alone as a live integration. Human acceptance or the current explicit acceptance policy closes the goal.
