# S1-05 — Connect Gemini 3.8 Live to real work and selected vision

**Human owner:** Davide  
**Design state:** `design_ready`  
**Implementation:** `not_started`  
**Dependencies:** S1-03, S1-04

## Product result

Both founders converse with Gemini 3.8 Live, launch real asynchronous work and review a selected visual target without losing conversation or duplicating jobs.

## Read before coding

Read the root AGENTS contract, then [06_VOICE_AND_VISION](../../architecture/06_VOICE_AND_VISION.md), [03_BACKEND](../../architecture/03_BACKEND.md). Resolve these source IDs in [the register](../../sources/source-register.json): G-01, G-02, G-03, G-04, G-05, G-06, GG-01, LK-01, LK-02, LK-03. Read the relevant pinned upstream implementation before changing that interface. These sources establish mechanisms; the proposed destination files are new Sophia code.

## Source destinations

- `apps/media-bridge/src/`
- `apps/studio/src/features/voice/`
- `apps/api/src/routes/voice/`

## Work sequence

1. Implement raw RTC subscription/publishing and Google SDK connection with explicit audio format conversion and bounded buffers.
2. Bind each admitted utterance/tool request to the authenticated input-floor epoch. Settle the current exchange generation before switching speakers.
3. Configure tools as NON_BLOCKING and place scheduling/willContinue at the actual FunctionResponse top level. Return admitted job identity, not fictional completion.
4. Add selected-frame input, exact source lookups, Stop Looking, playback epoch suppression, GoAway/warm resume and cold context recovery.

## Acceptance evidence

- Each founder takes the floor and receives a coherent audible response; both hear the same Sophia.
- A real dsh job continues while conversation continues, then yields one appropriate result notice.
- Selected visual context and exact source identity agree.
- A provider reconnect does not duplicate an admitted effect.

## Adverse checks

- Barge-in flushes old output; stale frames/audio are dropped.
- Late tool results are not attributed to a new speaker.
- Ending speech or observation does not stop an unrelated authorized build.

## Session and handback

Create the actual attempt binding at execution: goal ID, native session, owner, worktree/base commit, writable scope, runtime unit, existing authority and cumulative allowance. The brief is not a permission grant. Keep source and assignment identity in every handback. Use [SESSION_HANDOFF](../../templates/SESSION_HANDOFF.md); return commands actually run, exact result versions and limitations. A failed attempt remains part of this goal rather than disappearing when a new coding chat starts.
