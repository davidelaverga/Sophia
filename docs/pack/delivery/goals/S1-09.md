# S1-09 — Connect the three owner-operated engineering resources

**Human owner:** Davide  
**Design state:** `binding_audit_pending`  
**Implementation:** `not_started`  
**Dependencies:** S1-02

## Product result

Davide’s Codex, Davide’s Claude Code and Luis’s Claude Code each perform real work and expose their actual status and owner-specific native requests.

## Read before coding

Read the root AGENTS contract, then [05_EXECUTION_TEAMS](../../architecture/05_EXECUTION_TEAMS.md), [03_BACKEND](../../architecture/03_BACKEND.md), [10_OPERATIONS_AND_TESTS](../../architecture/10_OPERATIONS_AND_TESTS.md). Resolve these source IDs in [the register](../../sources/source-register.json): OM-01, OM-02, P-02, QM-02. Read the relevant pinned upstream implementation before changing that interface. These sources establish mechanisms; the proposed destination files are new Sophia code.

## Source destinations

- `packages/execution-adapters/src/omnigent/`
- `apps/api/src/routes/resources/`
- `apps/studio/src/features/work/`

## Work sequence

1. Complete the named Part 2 binding audit: exact current OpenAPI/session/feed/control/elicitation operations and owner authorization at the pinned Omnigent source.
2. Have each owner authorize launch/adoption of the selected native sessions and repo/worktree scope; keep vendor authentication native.
3. Implement capability-aware observations, amendment delivery and result collection through the narrow adapter.
4. Surface required native action with its exact resource/session/owner and reconcile resolution performed outside Sophia.

## Acceptance evidence

- All three actual resources are registered and each returns a useful bounded task result.
- A real permission request appears for the correct owner and resolves from native evidence.
- A locked but awake host does not require screenshot/click automation for supported programmatic operations.

## Adverse checks

- An edit grant does not permit arbitrary cross-owner host launch.
- A sleeping/offline host is reported, not treated as still progressing.
- Subscription or permission failure does not silently route through another person’s credentials.

## Session and handback

Create the actual attempt binding at execution: goal ID, native session, owner, worktree/base commit, writable scope, runtime unit, existing authority and cumulative allowance. The brief is not a permission grant. Keep source and assignment identity in every handback. Use [SESSION_HANDOFF](../../templates/SESSION_HANDOFF.md); return commands actually run, exact result versions and limitations. A failed attempt remains part of this goal rather than disappearing when a new coding chat starts.
