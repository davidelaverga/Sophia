# S3-02 — A useful Sophia-native application implementer

**Release:** Sprint 3 · **Owner:** Davide · **Implementation:** not_started

## User result

Deliver an actual end-to-end feature in the supported application stack using the same source, evidence, deployment and review contract as external engineers.

## Dependencies and reading

Dependencies: S3-01. Read [agent map](../../AGENTS.md), [02_DSH_BOOTSTRAP](../../architecture/02_DSH_BOOTSTRAP.md), [08_CREATION_AND_REVIEW](../../architecture/08_CREATION_AND_REVIEW.md), [14_RENDERER_EXTRACTION](../../architecture/14_RENDERER_EXTRACTION.md). Source IDs: DSH-01, DSH-16, DSH-18, OLD-05. Read the exact registered revision before coding the seam.

## Code destinations

- `packages/dsh-bundle/src/presets/app-worker/`
- `packages/build-runner/`
- `tests/native-app-feature/`

## Implementation sessions

### S3-02.1 — bounded work session

Extend the existing native React/TypeScript prototype profile into one supported web feature stack; retain the same repository/source and publication contracts as external engineers.

Finish with the actual source change, relevant checks, unresolved facts and the next session’s starting point. No session completion by itself closes this goal.

### S3-02.2 — bounded work session

Use a bounded task workspace with source, dependency lock, development-service grants and executable checks; no application/provider secrets enter generated code tools.

Finish with the actual source change, relevant checks, unresolved facts and the next session’s starting point. No session completion by itself closes this goal.

### S3-02.3 — bounded work session

Pair the economical dsh implementer with the already authorized stronger coordinator on a substantive feature; compare against a solo route on suitable work.

Finish with the actual source change, relevant checks, unresolved facts and the next session’s starting point. No session completion by itself closes this goal.

### S3-02.4 — bounded work session

Deliver data behavior, frontend integration, tests, usable preview and one user revision; document mocked services explicitly.

Finish with the actual source change, relevant checks, unresolved facts and the next session’s starting point. No session completion by itself closes this goal.

### S3-02.5 — bounded work session

Return exact source/evidence through the existing acceptance path. An external coordinator does not gain the native worker’s credentials.

Finish with the actual source change, relevant checks, unresolved facts and the next session’s starting point. No session completion by itself closes this goal.

## Acceptance evidence

- A Sophia-native worker delivers a supported end-to-end feature and its requested revision.
- The same project can use a native or external engineer without migrating its records.
- Checks run against the actual returned source.

## Adverse checks

- A prototype screenshot is not accepted as functioning application code.
- A missing development grant does not lead to production credential use.

## Handoff and acceptance

Use the [session handoff](../../templates/SESSION_HANDOFF.md). Retain the accepted source and prior usable output on failure. Record any operation whose external effect remains unknown; never present source edits alone as a live integration. Human acceptance or the current explicit acceptance policy closes the goal.
