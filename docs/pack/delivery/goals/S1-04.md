# S1-04 — Build the shared Studio shell and real room transport

**Human owner:** Luis  
**Design state:** `design_ready`  
**Implementation:** `not_started`  
**Dependencies:** S1-02

## Product result

Davide and Luis enter a real shared project, use independent lenses and hear one another while keeping one clear Sophia surface.

## Read before coding

Read the root AGENTS contract, then [04_FRONTEND](../../architecture/04_FRONTEND.md), [06_VOICE_AND_VISION](../../architecture/06_VOICE_AND_VISION.md). Resolve these source IDs in [the register](../../sources/source-register.json): P-01, P-02, LK-01, LK-02, LK-03. Read the relevant pinned upstream implementation before changing that interface. These sources establish mechanisms; the proposed destination files are new Sophia code.

## Source destinations

- `apps/studio/src/app/`
- `apps/studio/src/features/studio/`
- `apps/studio/src/features/voice/`
- `packages/ui/`

## Work sequence

1. Implement the custom React/Vite shell from the recovered Studio reference tokens and semantic map.
2. Implement current local lens/focus/drafts separately from shared project state. Preserve them during unrelated updates.
3. Issue scoped LiveKit room grants from the API; join from two independently authenticated clients.
4. Add explicit input-floor, invocation, end-exchange and room-leave controls; expose missing voice service honestly until S1-05.

## Acceptance evidence

- Changing Luis’s local view does not move Davide’s view or retask work.
- Two actual participants can talk, see membership and pass the input role.
- Required actions and persistent work controls remain accessible on narrow screens.

## Adverse checks

- Losing the UI connection does not cancel admitted background work.
- An unauthorized participant cannot reuse a room token for a different project.

## Session and handback

Create the actual attempt binding at execution: goal ID, native session, owner, worktree/base commit, writable scope, runtime unit, existing authority and cumulative allowance. The brief is not a permission grant. Keep source and assignment identity in every handback. Use [SESSION_HANDOFF](../../templates/SESSION_HANDOFF.md); return commands actually run, exact result versions and limitations. A failed attempt remains part of this goal rather than disappearing when a new coding chat starts.
