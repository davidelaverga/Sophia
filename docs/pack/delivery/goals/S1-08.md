# S1-08 — Import existing context and compile the next useful brief

**Human owner:** Davide  
**Design state:** `design_ready`  
**Implementation:** `not_started`  
**Dependencies:** S1-02

## Product result

A project continues from selected ChatGPT/Claude material and source files; the next task uses current accepted decisions and relevant lessons.

## Read before coding

Read the root AGENTS contract, then [09_MEMORY_AND_CONTEXT](../../architecture/09_MEMORY_AND_CONTEXT.md), [03_BACKEND](../../architecture/03_BACKEND.md). Resolve these source IDs in [the register](../../sources/source-register.json): P-01, P-02, QM-03. Read the relevant pinned upstream implementation before changing that interface. These sources establish mechanisms; the proposed destination files are new Sophia code.

## Source destinations

- `packages/context/`
- `apps/worker/src/jobs/import/`
- `apps/studio/src/features/knowledge/`

## Work sequence

1. Implement selective import with an inclusion/omission preview and explicit project release; retain source provenance.
2. Separate accepted project records from imported advice/history and derived wiki pages.
3. Implement direct current-state reads, scoped source search/read and a bounded ContextPacket with exact revisions.
4. Implement correction/Forget influence fencing and one cycle-close learning that actually enters a later relevant brief.

## Acceptance evidence

- The team corrects an imported project summary before making it accepted context.
- A next-session brief uses an earlier accepted decision or lesson with its source.
- Private material remains excluded until its owner releases the exact contribution.

## Adverse checks

- An old model answer cannot override a newer project decision.
- Revoked content cannot re-enter through a cached packet, offloaded page or resumed worker.
- Missing export attachments are reported, not reconstructed as facts.

## Session and handback

Create the actual attempt binding at execution: goal ID, native session, owner, worktree/base commit, writable scope, runtime unit, existing authority and cumulative allowance. The brief is not a permission grant. Keep source and assignment identity in every handback. Use [SESSION_HANDOFF](../../templates/SESSION_HANDOFF.md); return commands actually run, exact result versions and limitations. A failed attempt remains part of this goal rather than disappearing when a new coding chat starts.

## Part 2 binding documents

[12_DATA_AND_API_BINDINGS](../../architecture/12_DATA_AND_API_BINDINGS.md). Use [integration sessions](../sessions/S1_BINDING_SESSIONS.md) for the actual live proof. Reference-code tests are not substitutes for native/provider evidence.
