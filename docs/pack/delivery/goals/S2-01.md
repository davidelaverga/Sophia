# S2-01 — Semantic attention and milestone cooperation

**Release:** Sprint 2 · **Owner:** Luis · **Implementation:** not_started

## User result

Use the heartbeat and actual artifact milestones to create source-bound opportunities; retain separate required actions, quiet progress and controls.

## Dependencies and reading

Dependencies: S1-14. Read [agent map](../../AGENTS.md), [13_FRONTEND_BINDINGS](../../architecture/13_FRONTEND_BINDINGS.md), [12_DATA_AND_API_BINDINGS](../../architecture/12_DATA_AND_API_BINDINGS.md). Source IDs: UI-01, BZ-01, QM-01. Read the exact registered revision before coding the seam.

## Code destinations

- `apps/api/src/services/attention.ts`
- `apps/studio/src/features/attention/`
- `packages/dsh-bundle/src/lead/milestone-review.ts`

## Implementation sessions

### S2-01.1 — bounded work session

Extend the existing lead review output with none or a captured milestone/choice; do not add a second always-running project manager.

Finish with the actual source change, relevant checks, unresolved facts and the next session’s starting point. No session completion by itself closes this goal.

### S2-01.2 — bounded work session

Persist the opportunity with exact artifact/source, relevance window, intended audience and current authorized no-response continuation.

Finish with the actual source change, relevant checks, unresolved facts and the next session’s starting point. No session completion by itself closes this goal.

### S2-01.3 — bounded work session

Render Work Pulse, owner-required HumanAction and optional CooperationOpportunity as different types over one project event stream.

Finish with the actual source change, relevant checks, unresolved facts and the next session’s starting point. No session completion by itself closes this goal.

### S2-01.4 — bounded work session

Add Accept/Change/Reflect/Later/Decline operations appropriate to that type; viewer dismissal changes attention, not authority.

Finish with the actual source change, relevant checks, unresolved facts and the next session’s starting point. No session completion by itself closes this goal.

### S2-01.5 — bounded work session

Measure helpfulness, wrong/missed detections and resulting rework separately; do not convert a click into a correctness label.

Finish with the actual source change, relevant checks, unresolved facts and the next session’s starting point. No session completion by itself closes this goal.

## Acceptance evidence

- A real useful milestone is captured and shown without replacing a newer candidate.
- An optional invitation can be declined while a separate required action remains unresolved.
- A no-op review produces no distracting card.

## Adverse checks

- A stale screenshot cannot appear as the live milestone.
- A dismissed card cannot approve a permission or mission change.

## Handoff and acceptance

Use the [session handoff](../../templates/SESSION_HANDOFF.md). Retain the accepted source and prior usable output on failure. Record any operation whose external effect remains unknown; never present source edits alone as a live integration. Human acceptance or the current explicit acceptance policy closes the goal.
