# S2-04 — In-app native requests and mobile control

**Release:** Sprint 2 · **Owner:** Davide · **Implementation:** not_started

## User result

Answer supported native permission requests from authenticated Sophia mobile/desktop; retain exact native handoff for unsupported login/OS actions.

## Dependencies and reading

Dependencies: S1-14. Read [agent map](../../AGENTS.md), [11_OMNIGENT_BINDINGS](../../architecture/11_OMNIGENT_BINDINGS.md), [13_FRONTEND_BINDINGS](../../architecture/13_FRONTEND_BINDINGS.md). Source IDs: OM-03, OM-04, OM-05, OM-09, CC-01. Read the exact registered revision before coding the seam.

## Code destinations

- `apps/api/src/services/human-actions.ts`
- `apps/studio/src/features/mobile/`
- `apps/api/src/services/push.ts`

## Implementation sessions

### S2-04.1 — bounded work session

Expose only schema-bound supported native permission response modes. Preserve the exact owner, native request ID, source fingerprint and expiry.

Finish with the actual source change, relevant checks, unresolved facts and the next session’s starting point. No session completion by itself closes this goal.

### S2-04.2 — bounded work session

Authenticate the mobile action, re-read current request and assignment authority, then call the native resolve route once.

Finish with the actual source change, relevant checks, unresolved facts and the next session’s starting point. No session completion by itself closes this goal.

### S2-04.3 — bounded work session

Use the owner-delegated Omnigent connection; default approval is one-time and never silently supplies persistence metadata.

Finish with the actual source change, relevant checks, unresolved facts and the next session’s starting point. No session completion by itself closes this goal.

### S2-04.4 — bounded work session

Add PWA web-push with a minimal authenticated deep link. Keep the same durable HumanAction independent of push delivery/read receipts.

Finish with the actual source change, relevant checks, unresolved facts and the next session’s starting point. No session completion by itself closes this goal.

### S2-04.5 — bounded work session

Support manual progress review, current preview, comment, steer and Hold/Stop on mobile. Native login/OS/2FA requests remain explicit handoffs.

Finish with the actual source change, relevant checks, unresolved facts and the next session’s starting point. No session completion by itself closes this goal.

## Acceptance evidence

- The correct owner resolves one supported request from mobile and its native resolution is observed.
- An unsupported native request remains visible with an accurate handoff.
- A mobile steer reaches the currently authorized worker through the lead.

## Adverse checks

- A stale tap after Stop cannot grant a superseded operation.
- A member cannot answer a different owner request by reusing its URL.
- A lost native response is not silently retried as a fresh grant.

## Handoff and acceptance

Use the [session handoff](../../templates/SESSION_HANDOFF.md). Retain the accepted source and prior usable output on failure. Record any operation whose external effect remains unknown; never present source edits alone as a live integration. Human acceptance or the current explicit acceptance policy closes the goal.
