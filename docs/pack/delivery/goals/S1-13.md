# S1-13 — Deliver useful reports, PDFs and decks through isolated render jobs

**Human owner:** Davide  
**Design state:** `binding_audit_pending`  
**Implementation:** `not_started`  
**Dependencies:** S1-03, S1-06

## Product result

Sophia produces a meaningful sourced report and presentation, with retained images/source and actual usable exports.

## Read before coding

Read the root AGENTS contract, then [08_CREATION_AND_REVIEW](../../architecture/08_CREATION_AND_REVIEW.md), [10_OPERATIONS_AND_TESTS](../../architecture/10_OPERATIONS_AND_TESTS.md). Resolve these source IDs in [the register](../../sources/source-register.json): OLD-01, OLD-02, OLD-03, P-01. Read the relevant pinned upstream implementation before changing that interface. These sources establish mechanisms; the proposed destination files are new Sophia code.

## Source destinations

- `renderers/python/`
- `renderers/web/`
- `apps/worker/src/jobs/render/`
- `packages/creative/`

## Work sequence

1. Finish the Part 2 renderer closure audit beginning at the verified legacy tool entry points; locate the real rich HTML-to-PDF and deck compilation functions and their dependencies.
2. Extract format-specific code into isolated jobs with explicit source inputs/results; replace ToolRuntime/thread path injection at the adapter boundary.
3. Integrate selected image assets and source manifests. Preserve source versus rasterized export distinctions.
4. Validate actual PDF/PPTX files and their visual rendering; register candidates through the ordinary artifact path.

## Acceptance evidence

- One substantive report and one deck are generated from retained source and readable in the app.
- The files open correctly and include the intended images and sources.
- The normal memory/authority route works; no special bypass admission is used.

## Adverse checks

- A renderer failure preserves prior output and returns diagnostic evidence.
- The small legacy PDF smoke wrapper is not presented as a complete rich renderer.
- An editable-source claim is not made for flattened raster output.

## Session and handback

Create the actual attempt binding at execution: goal ID, native session, owner, worktree/base commit, writable scope, runtime unit, existing authority and cumulative allowance. The brief is not a permission grant. Keep source and assignment identity in every handback. Use [SESSION_HANDOFF](../../templates/SESSION_HANDOFF.md); return commands actually run, exact result versions and limitations. A failed attempt remains part of this goal rather than disappearing when a new coding chat starts.
