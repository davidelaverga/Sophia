# S2-02 — Exact source-backed artifact revision

**Release:** Sprint 2 · **Owner:** Luis · **Implementation:** not_started

## User result

Integrate expected-base and component-scoped mutations, preserved-sibling checks and atomic candidate publication for supported artifacts.

## Dependencies and reading

Dependencies: S1-14. Read [agent map](../../AGENTS.md), [14_RENDERER_EXTRACTION](../../architecture/14_RENDERER_EXTRACTION.md), [13_FRONTEND_BINDINGS](../../architecture/13_FRONTEND_BINDINGS.md). Source IDs: OLD-02, OLD-03, OLD-07, UI-01. Read the exact registered revision before coding the seam.

## Code destinations

- `packages/artifacts/src/mutations/`
- `apps/api/src/services/artifact-publication.ts`
- `apps/studio/src/features/review/`

## Implementation sessions

### S2-02.1 — bounded work session

Start with source-backed UI/HTML and slide-HTML components; retain originals for imported flattened documents. Do not claim reverse editability from pixels.

Finish with the actual source change, relevant checks, unresolved facts and the next session’s starting point. No session completion by itself closes this goal.

### S2-02.2 — bounded work session

Bind an edit to expected source/version, target component IDs and protected sibling source hashes; store a candidate, never mutate the stable bundle in place.

Finish with the actual source change, relevant checks, unresolved facts and the next session’s starting point. No session completion by itself closes this goal.

### S2-02.3 — bounded work session

Run the worker in a bounded source workspace, validate the complete diff and reject out-of-scope changes before publication.

Finish with the actual source change, relevant checks, unresolved facts and the next session’s starting point. No session completion by itself closes this goal.

### S2-02.4 — bounded work session

Re-render affected outputs and verify declared appearance/behavior constraints; packaging metadata and legitimate pagination are not byte-equality failures.

Finish with the actual source change, relevant checks, unresolved facts and the next session’s starting point. No session completion by itself closes this goal.

### S2-02.5 — bounded work session

Use the same publication CAS for Luis edits and agent edits. Cross-cutting layout requests require an explicit expanded mandate, not silent scope widening.

Finish with the actual source change, relevant checks, unresolved facts and the next session’s starting point. No session completion by itself closes this goal.

## Acceptance evidence

- One requested component changes while protected sibling source hashes remain unchanged.
- The target is corrected in co-review before a consequential ambiguous edit.
- The image-based PPTX is revised through retained HTML and re-exported with notes.

## Adverse checks

- A stale source edit loses CAS without overwriting the newer accepted artifact.
- An out-of-scope diff is rejected and the previous preview stays available.
- An arbitrary PDF import is not called natively editable.

## Handoff and acceptance

Use the [session handoff](../../templates/SESSION_HANDOFF.md). Retain the accepted source and prior usable output on failure. Record any operation whose external effect remains unknown; never present source edits alone as a live integration. Human acceptance or the current explicit acceptance policy closes the goal.
