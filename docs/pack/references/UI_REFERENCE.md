# Concrete Studio reference

**Primary visual/semantic reference:** `Sophia_Studio_V1_Evolved_V2_R1.html` from Library/Sophia, recovered in the v0.2 study.

- Library file identity: `file_000000004da881fd844426d1f13e9665`.
- Recorded size: 1,804,632 bytes.
- SHA-256: `f3879d9e3d43bdfe5f14fc1472908614b0d17d054a8ae013a80a63166f1ddc23`.
- Prior study rendered the initial desktop/mobile views; this pack does not claim a new full walkthrough validation.

The reference is an offline illustrative HTML, not a deployed shared application. Its context, actions, account states and test results are fixtures. Retain the quiet dark/violet surfaces, orb, readable hierarchy, large artifact canvas and optional contextual inspection. Implement the production behavior in React against the real contracts.

Preserve independent local lens/shared focus; persistent Conversations and Goals; exact source/preview identity; owner-addressed HumanActions; optional cooperation distinct from required actions; cross-lens bridges over the same object; honest failure, return and native resolution states. “Show in workspace” remains a deliberate navigation action, not an unrelated event causing a focus jump.

The underlying UI iteration plan and v0.2 study explain the behavior. Latest user choices move exact edits to S2 and make all three real engineers part of S1; illustrative historical provider/owner assignments do not override those choices. [P-02]

The previous v0.2 review pack retains its reference captures. This pack identifies the reference rather than multiplying large embedded HTML copies. The implementing UI goal must retrieve that exact source from the Library or supplied review archive, not reconstruct a supposedly identical reference from a screenshot.
