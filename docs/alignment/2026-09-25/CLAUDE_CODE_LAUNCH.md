# Claude Code launch — S1-05A

Paste the block below into the implementation session with this packet available. This starts implementation and evidence collection, not automatic production changes. Record the actual local packet path before using links.

```text
You are the implementation lead for S1-05A in davidelaverga/Sophia.

Goal: make Luis's current shared room a real Gemini 3.8 Live conversation and connect one useful, authorized, durable dsh task to it. Preserve the existing product direction and all reviewed work. Do not restart the architecture or rebuild the room.

Read the supplied S1-05A_GOAL_SPEC.md, GOAL_SPEC_AMENDMENTS.md, PROGRESS_AND_ALIGNMENT_LEDGER.md and CLAUDE_CODEX_PROTOCOL.md. Then read the repository's AGENTS.md/CLAUDE.md/CONTRIBUTING.md, current source/destination maps, the original S1-02/S1-03/S1-04/S1-05 briefs and active contract amendments. The supplied packet is an additive implementation proposal; docs/pack remains frozen.

At review time main was 01d9117bdcf9ec8ee18cd5414aa08ee4f24265a3; the cumulative baseline was studio/qol at 29a570c33feb97a6bc04ea357c84087a47c9b055, including all of main and the rest of Luis's stack. Refresh every ref and PR state. Preserve newer legitimate work. Work in a dedicated branch/worktree from the agreed cumulative baseline, not old main. Coordinate shared contract/migration ownership and merge review with Luis. No blind merge, reset, rebase, or force-push of his stack.

Begin by reporting the actual baseline and a short checkpoint plan, then implement. Close the missing production /v1/runtime service boundary, validate canonical wire types, reuse the existing bridge/journal/supervisor, and admit one source-bound draft_brief task through the real API. Wire attributed discussion and the actual media bridge into Luis's room. Use the original voice/media contract with only implemented tools exposed. Keep room speech, AI input, work authority and guest access distinct. Preserve A01 and add reviewed amendments rather than duplicating the floor.

The light must reflect actual AI/media/work state. A LiveKit connection is not evidence that Google is hearing the room. Project-aware Sophia must be quiesced before guest admission. A failed LiveKit removal remains pending and is reconciled. The source spec gives the exact role, audience and recovery behavior.

You may implement source, local migrations and tests under your existing development authority. You do not have authority to deploy production, apply hosted schemas, alter production secrets/Auth settings, or bypass a denial. Do not copy Codex credentials or invoke a higher-privilege child to evade those limits.

Use the supplied protocol with a separately initialized Codex operator. First request a read-only hosted baseline inspection and flag the reported old Render auto-deploy branch. Prepare exact reviewed operation requests only when needed: commit, migration hashes, target, preconditions, verification and rollback. An execution request is not an approval. Continue independent local work while an operator action is pending.

Use a small number of implementation checkpoints with real user-visible behavior. Run current checks and the affected regressions. A paid model/media test requires an explicit owner allowance and an authorized execution-host key; missing approval is not permission to substitute a mock and call it live.

Finish with changed files/commits, actual tests, acceptance-case results, operator receipts and remaining boundaries. Distinguish candidate ready from integrated/hosted acceptance. Keep unfulfilled original goal criteria open. Do not expand into a full PM, image/prototype platform or external executor product to complete this slice.
```
