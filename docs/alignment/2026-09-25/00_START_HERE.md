# Sophia — current progress and next integration goal

**25 September 2026 · v1.0 · review and executable planning packet**

## Start here

For Davide and Luis: [progress and alignment ledger](PROGRESS_AND_ALIGNMENT_LEDGER.md), followed by [the next goal](S1-05A_GOAL_SPEC.md).

For the implementation agent: [Claude launch](CLAUDE_CODE_LAUNCH.md), [S1-05A](S1-05A_GOAL_SPEC.md), [goal amendments](GOAL_SPEC_AMENDMENTS.md), then the original source-linked repository instructions/specs.

For the authorized operations agent: [Codex launch](CODEX_OPERATOR_LAUNCH.md) and [communication protocol](CLAUDE_CODEX_PROTOCOL.md).

For verification: [source register](SOURCE_REGISTER.md), [branch/commit register](evidence/BRANCH_COMMIT_REGISTER.md), and [video review](evidence/VIDEO_REVIEW.md).

## The recommended next step

**Make the existing room useful through real conversation and one durable dsh task.** Preserve Luis's additions, close the real API/runtime integration, bind the visual presence to actual media state, and keep hosted effects on the explicitly authorized operator path.

Current inspection anchors: `main@01d9117b`, `studio/qol@29a570c3`. The stack contains 45 additional commits above main. Main is not the full product; the draft stack is not automatically approved; a reported hosted commit is not a freshly verified deployment.

## Scope and delivery

This packet adds a proposed S1-05A goal and amendment crosswalk. It does not edit the repository or imported plan. It creates no issues, PRs, merges, tasks, deployments, migrations or new authority grants. The exact next launch may adopt its proposed product decisions; until then they are recommendations.

Copy the entire packet into a repository-owned documentation folder such as `docs/alignment/2026-09-25/` when implementing. Preserve relative links between its files; original repository references use pinned GitHub URLs. Do not copy it into the byte-frozen `docs/pack/` directory. Ignore older superseded planning approaches when they conflict with the selected dsh/voice-first architecture.

The ZIP is the portable coding-agent handoff. `READER.html` is a self-contained human reader; its navigation works without extracting supporting Markdown files. Local validation checks cover links and file integrity, not live application correctness.
