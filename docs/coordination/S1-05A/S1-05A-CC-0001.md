# S1-05A-CC-0001: read-only hosted baseline inspection

```yaml
protocol: sophia.dev-handoff.v1
message_id: S1-05A-CC-0001
from_role: implementer
reply_to: null
kind: inspect_request
operation_id: S1-05A-OP-0001
candidate_commit: null
summary: Inspect the current hosted deployment, schema ledger and settings baseline. Read only; no changes.
```

## What to resolve (read only)

1. **Render.** The service(s) actually serving the Sophia API (the public report names `sophia-next`): service id, the branch it tracks, whether auto-deploy is on, the latest deploy id and its source commit, and the health/readiness endpoints it exposes. Confirm the identity against the authorized account; do not pick a similarly named service.
2. **Studio (Vercel).** Project, production deployment id and its source commit.
3. **Database.** The hosted migration ledger (file name and checksum for 0001 onward) as the repository's runner records it. Compare it with the repository's `db/migrations/0001…0011` checksums. Report the API and migration roles as boolean facts (distinct, non-owner, non-BYPASSRLS), and whether the connection uses the session-mode pooler with `verify-full` TLS.
4. **Settings (presence only, never values):** `INVITE_TOKEN_SECRET`, `STUDIO_URL`/`STUDIO_ORIGINS`, `LIVEKIT_URL`/`LIVEKIT_API_KEY`/`LIVEKIT_API_SECRET`, `RESEND_API_KEY`/`INVITE_FROM`, the Supabase JWT issuer/JWKS settings. Report each as `set`/`unset`.
5. **LiveKit.** Whether a LiveKit Cloud project exists for this deployment and is reachable from the API host (status only).
6. **Supabase Auth.** Public sign-ups enabled or disabled; anonymous sign-ins enabled or disabled; redirect URLs as configured (hosts only).

## Flagged hazard

Luis's dated report says Render auto-deploys `deploy/render-vercel`. That branch is `ff22a249`, an ancestor of `studio/qol@29a570c3` without the room and access work. A push to it would replace the room-capable API with an older one. Please report the actual setting. **Do not change it under this request.** If the setting is as reported, the implementer will send a bounded `execution_request` to turn auto-deploy off for the alignment release (a temporary release procedure, not a permanent policy), and the owner decides.

## Not requested

No deploy, migration, environment edit, Auth change, API key creation, service creation, plan change, or synthetic-auth test (those create and delete records and need their own request).

## Expected reply

A `result` message (protocol §8) with what was actually observed, what stayed unknown, and any immediate release hazard. Sanitized: no tokens, URLs with secrets, `.env` contents or database rows.
