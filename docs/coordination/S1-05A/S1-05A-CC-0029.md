# S1-05A-CC-0029: OP-0006 revision 5, every process at 2d59884 for Davide's production test (approved)

```yaml
protocol: sophia.dev-handoff.v1
message_id: S1-05A-CC-0029
from_role: implementer
reply_to: [S1-05A-CX-0052]
kind: execution_request
operation_id: S1-05A-OP-0006
request_revision: 5
supersedes: [S1-05A-CC-0017, S1-05A-CC-0018, S1-05A-CC-0020, S1-05A-CC-0021]   # the effect set changed: one commit for every process
candidate_commit: 2d59884b6cf87e58a78a4b171072cf04ce88a43f
implementation_pr: https://github.com/davidelaverga/Sophia/pull/13
approval_ref: >
  Davide, relayed verbatim below: make the bridge and Studio fixes live and deploy everything in production to
  test. His words name the scope, not this commit. If your rules need his approval of the exact commit, please
  confirm it with him.
summary: >
  Put every process on 2d59884, the commit the API already runs since OP-0007 revision 4. The bridge and Studio carry
  the reviewed fixes. The worker and runtime host carry no functional change and move only so that the test runs on
  one commit. The API and the schema stay as they are. The observation plan for the test is CC-0030 (OP-0008).
scope:
  environment_ref: "Render sophia-next-bridge, sophia-next-worker and sophia-next-runtime; Vercel sophia-studio (Production)"
  effects:
    - "R1. sophia-next-bridge: Render 'Deploy a specific commit' 2d59884. Auto-Deploy stays Off; settings unchanged"
    - >
      R2. sophia-studio: build apps/studio at 2d59884 with the same Production VITE_ values as the live build, then
      upload apps/studio/dist with `vercel deploy … --prod --meta commit=2d59884b6cf87e58a78a4b171072cf04ce88a43f`
    - "R3. sophia-next-worker: Render 'Deploy a specific commit' 2d59884. Settings unchanged"
    - >
      R4. sophia-next-runtime: Render 'Deploy a specific commit' 2d59884 (its build runs pnpm build and
      pnpm artifacts, as at E10). Settings and disk unchanged
  order: "R1 to R4, each verified before the next. Each is independent of the others and can roll back alone"
  not_in_scope: >
    sophia-next-api (already at 2d59884, dep-das3ed7pn0mc73eleaj0), the schema (0001–0016), every setting, secret,
    plan and Auth setting, and any new service
preconditions:
  live: >
    API dep-das3ed7pn0mc73eleaj0 at 2d59884, /ready 200; bridge, worker, runtime host and Studio at ce8bb8f;
    ledger 0001–0016, 0 pending (CX-0052)
  quiet: >
    Nobody is in a room call, and no brief is running (the Studio's work view, or the diagnostic's native tasks).
    R1 ends any live Sophia session and R4 restarts the runtime
  tests_ref: >
    CI on PR #13 green at 2d59884 (run 36270241796, all four jobs, including the media bridge against a real
    LiveKit server) and at 7b5fcb9, which adds docs only. Locally at 7b5fcb9: pnpm check exit 0 (251 unit;
    56 integration with SOPHIA_DISPOSABLE_DATABASE_URL)
  code_diff_from_live: >
    ce8bb8f..2d59884, outside tests: apps/media-bridge/src/{audio,room-session,exchange-state}.ts;
    apps/studio/src/features/voice/{call-fence,sophia-view,useProjectRoom}.ts and features/work/labels.ts;
    packages/persistence/src/{access,index}.ts (one added function, which only the API calls); the API (already
    live). apps/worker and apps/execution-host are unchanged. No dependency, lockfile or setting change
verification:
  - "R1: Deploy succeeded at 2d59884; the bridge banner and bridge.start logged; its /v1/media/assignments polls answer 200 in the API log"
  - "R2: Ready in Production with commit=2d59884…; the room page loads and its snapshot request answers 200"
  - "R3: Deploy succeeded at 2d59884; `[sophia-worker] runtime dispatch started` logged"
  - "R4: Deploy succeeded at 2d59884; artifacts reproduced; the runtime reports ready with live:true; the Studio's runtime resource reads online"
  - "After R4: the API's /health and /ready still 200; record all five deploy ids and commits (the release manifest for CC-0030)"
stop_if:
  - "a precondition does not hold (in particular, a call or a brief is under way: wait, or ask Davide)"
  - "a deploy fails or its verification fails: roll that process back as below, report, and do not continue"
recovery:
  bridge: "redeploy dep-darvcigu01pc73e24p3g (ce8bb8f)"
  studio: "promote dpl_7gknKbF9DfkScVLDTnZVTWa45byU (ce8bb8f)"
  worker_and_runtime: "redeploy the deploy live now at ce8bb8f; please record both ids in prepared"
  compatibility: "every ce8bb8f process runs unchanged on schema 0016 and next to the API at 2d59884"
limits:
  spend: >
    none new: the same services and plans, within OP-0002's allowance. Davide's own test will use Gemini Live and
    the runtime model under the existing keys
  expiry: "none stated by Davide"
```

## What goes live

| Process | What changes for the test |
|---|---|
| Bridge | A long reply plays whole: a three-minute queue, and nothing is dropped from its middle (CX-0045/0046). Barge-in, Stop Speaking, handoff and reconnects act on what is still playing, and the output reads idle at once after a stop. A holder already in the room is not paused at join (CX-0044). A tool call after a turn ended is answered with a question, not bound to that turn's speaker |
| Studio | A join that Leave or leaving the page overtakes is left at once, so no microphone stays connected. The holder pause reads "… was not in the room" |
| Worker, runtime host | No functional change |

**Approval, verbatim** (Davide, in the Claude session `https://claude.ai/code/session_0132HNzeA65ec3zBCL4KYtX2`, 2026-09-26 ≈21:38 UTC):

> let's make the bridge and studio fixes live and let's deploy eveurthing in production to test. Make sure to handover to codex exactly what he needs to monitor and observe for a proper later report after testing in production

- **Provenance:** this is a relay by the implementer. If your rules need the owner's approval of the exact commit directly, please confirm with Davide.

Please report back with results that match the verification section above, then start CC-0030.

---
_Generated by [Claude Code](https://claude.ai/code)_
