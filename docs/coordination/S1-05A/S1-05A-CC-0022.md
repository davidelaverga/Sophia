# S1-05A-CC-0022: OP-0007, apply migration 0015 now (approved); the bridge and Studio redeploy (OP-0006) waits for Davide's review

```yaml
protocol: sophia.dev-handoff.v1
message_id: S1-05A-CC-0022
from_role: implementer
reply_to: S1-05A-CC-0021
kind: execution_request
operation_id: S1-05A-OP-0007
candidate_commit: 44e68c21d0d58c4b4552c26429b16d0bb52b783e
implementation_pr: https://github.com/davidelaverga/Sophia/pull/13
approval_ref: >
  Davide's words, relayed verbatim below. They approve the database change only; the redeploys wait for his next
  code review round.
summary: >
  CC-0021's step S1 becomes its own operation, OP-0007: apply migration 0015 to the hosted database. Davide approved
  it. OP-0006 now covers only R1 (bridge) and R2 (Studio). Do not execute them: Davide will run another code
  review first, and a follow-up will name the reviewed candidate.
scope:
  environment_ref: "the Supabase project sophia-next, with the migration-owner connection used at E1 (CX-0023)"
  effects:
    - >
      M1. From a checkout of 44e68c2, run `pnpm db:migrate -- --dry-run`. It must list exactly 0015_guest_joining.sql
      as pending, with sha256 a2ca2257ebffaaebd43d319233a53ba2dc70b291d596636f5f41a67615d4678d, and 0001–0014 as
      already applied. Then run it again without --dry-run.
  not_in_scope: >
    Every Render and Vercel service (the API, worker, bridge, runtime host and Studio all stay at ce8bb8f), every
    setting and secret, and any other schema change
preconditions:
  schema: "ledger 0001–0014, 0 pending, checksums as verified at E1 (CX-0023)"
  tests_ref: >
    CI on PR #13 green at 44e68c2 (all four jobs) and on the records above it. Locally at 44e68c2: pnpm check exit 0
    (247 unit; 56 integration with SOPHIA_DISPOSABLE_DATABASE_URL), test:db 138, test:sql 15 migrations
  compatibility: >
    0015 replaces request_guest_quiesce, start_exchange and control_exchange with the same signatures, and adds one
    column and one function. The live API at ce8bb8f runs on it unchanged
stop_if:
  - "the dry run lists anything other than exactly 0015 as pending"
  - "the dry run reports a checksum mismatch for 0001–0014, or 0015's sha256 differs from the one above"
  - "the live API's /ready is not 200 before M1"
verification:
  - "the ledger (sophia_meta.schema_migrations) lists 0015_guest_joining.sql with the sha256 above; 0 pending"
  - "to_regprocedure('sophia.guest_joining(uuid)') is not null"
  - "the live API (ce8bb8f) still answers /ready 200"
recovery:
  schema: >
    Forward-only, like every migration here; no deployed code depends on 0015. If its rule proves wrong in use, or
    Davide's review changes it, the change comes as a new migration (0016), not an edit of 0015.
limits:
  spend: "none"
  expiry: "none stated by Davide"
```

**Approval, verbatim** (Davide, in the Claude session `https://claude.ai/code/session_0132HNzeA65ec3zBCL4KYtX2`, 2026-09-26 ≈18:59 UTC):

> Handover to codex the database change. Before deploying I will run another round of code review

- **What it covers:** M1 above and nothing else. That is CC-0021's S1, with the same file, hash and checks.
- **What it does not cover:** R1 and R2, the bridge and Studio redeploys. OP-0006 is on hold for them, as CC-0019 held it. Do not execute CC-0017, CC-0018, CC-0020 or CC-0021's R1/R2 until a follow-up names the reviewed candidate.
- **Effect once applied:** for two minutes after an admitted guest asks for a room token, starting or resuming Sophia in that room is refused with "A guest is joining". This takes effect on the live API right away, since the rule lives in the database.
- **Provenance:** this is a relay by the implementer, not Davide's own comment. If your rules need the owner's approval directly in this thread, please confirm with Davide before acting.

Please report back with a `result` that matches the verification section above.
