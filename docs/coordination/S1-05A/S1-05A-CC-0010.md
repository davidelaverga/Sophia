# S1-05A-CC-0010: execution request, revision 3 of the S1-05A release (OP-0002): three Starter services, the API stays free

```yaml
protocol: sophia.dev-handoff.v1
message_id: S1-05A-CC-0010
from_role: implementer
reply_to: S1-05A-CC-0009
kind: execution_request
operation_id: S1-05A-OP-0002
request_revision: 3
supersedes: [S1-05A-CC-0008, S1-05A-CC-0009]
candidate_commit: 0b9a8d0e607a31ea6ae96d907487fe3ea6c60412
implementation_pr: https://github.com/davidelaverga/Sophia/pull/13
approval_ref: >
  Davide's words, relayed verbatim below. They name the deploy and the three Starter services, not every
  effect or limit; the operator confirms the rest with Davide directly (see "Owner's words").
summary: >
  Apply 0012–0014, create the worker login and the runtime and bridge capabilities, and deploy the API (on its
  current free instance) and Studio at the candidate. Then add three Render background workers on the Starter
  plan (worker, media bridge, runtime host), each taking from the API service only the settings it needs.
  Smoke checks make no paid model call.
scope:
  environment_ref: >
    Render (Sophia's workspace; API sophia-next-api srv-daqrn08473hc73flhi8g), Supabase project sophia-next,
    Vercel project sophia-studio, LiveKit project p_40misr7kwz5, as observed in CX-0004 to CX-0013
  effects: see "Ordered effects" below (E1–E10); nothing else
  not_in_scope: >
    the API's instance plan, Supabase Auth settings (anonymous sign-ins), INVITE_FROM, LiveKit PII redaction,
    any other service, a schema reversal, and any paid model or media call during the smoke checks
preconditions:
  api_live: "dep-darb9e6gekts738ucd0g from 878aa9e6f1707c1d697b7cfe7d7d6e0b0c61de7c, branch studio/qol, free instance"
  auto_deploy: "Off on sophia-next-api (CX-0013)"
  studio_live: "Production JDM4Ng5TjbcyzQraiHtNFhmDurvP (CX-0010), or a later studio/qol build; record what is live"
  api_settings: >
    LIVEKIT_URL, LIVEKIT_API_KEY and LIVEKIT_API_SECRET nonblank; STUDIO_ORIGINS the Studio origin; HOST 0.0.0.0;
    the Google and OpenAI keys present (CX-0004, CX-0005, CX-0009)
  schema: "ledger 0001–0011 (fingerprint ee2f78839c2d14fb04a9376699820c7f5b04cb6bf82eb847991e174fff6ce2cf)"
  tests_ref: >
    CI on PR #13 at the candidate: all four jobs green in runs 36174908843 and 36174904141. Locally: pnpm check
    exit 0 at 0b9a8d0 (223 unit, 56 integration against the real pinned dsh); test:db 136 and test:livekit 2/2 at
    36cd1f2, whose API, worker, bridge, scripts and SQL are identical; test:sql (14 migrations) at c99fd7a.
  stop_if:
    - the candidate commit or its CI state differs from the above
    - the API's live deploy, branch or Auto-Deploy setting differs from the above
    - the ledger fingerprint differs, or the dry run lists anything other than exactly 0012, 0013, 0014 with these checksums
    - a deploy of any commit other than the candidate starts on sophia-next-api (cancel it; see E6)
    - pnpm artifacts on the runtime host does not reproduce config/runtime-unit.json's recorded identities
    - /ready is not 200 after the API deploy (then the rollback below; the schema stays)
inputs:
  migration_files: [0012_runtime_service.sql, 0013_room_exchange.sql, 0014_room_removals.sql]
  migration_sha256:
    0012_runtime_service.sql: 0b4c973d28273ff2567f88bf95b5a513299adb3b2973e3ffa0d33dab8578a60f
    0013_room_exchange.sql: 44ac2a681e4179a2a7649d632f0382fff32b412a9d113d3d9b1db62521bad221
    0014_room_removals.sql: 55948614ffb026fa040139a140518e8ee378055b28346ad953fc09354e65cfe2
  release_manifest_ref: deploy/S1-05A-release.md at the candidate commit
  owner_inputs:
    - "the project the runtime serves (its id) and an admin email of that project: from Davide privately, never on this issue"
verification:
  expected_schema: "0001–0014 applied; the runner reports 0 pending; 0012–0014 carry the checksums above"
  expected_deployments:
    - "sophia-next-api live at the candidate commit, still on its free instance (a new deploy id, recorded)"
    - "sophia-studio production built from the candidate commit (deploy id and commit recorded)"
    - "sophia-next-worker, sophia-next-bridge and sophia-next-runtime live at the candidate on Starter (service and deploy ids recorded)"
  smoke_checks: see "Smoke checks" below (S1–S6)
recovery:
  compatible_code_rollback: >
    Roll back the API and Studio as a pair: API to dep-darb9e6gekts738ucd0g (878aa9e6), branch back to studio/qol;
    Studio to the deployment recorded as live before E7. 878aa9e6's own test:db passes 84 of 86 on 0001–0014; the
    two differences are amendment A06's floor rule. Suspend the three new services; nothing else depends on them.
  schema_reversal: "not authorized: 0012–0014 stay"
limits:
  expires_at: null                  # not stated by Davide; confirm with him
  spend_ceiling: >
    Davide chose three Starter background workers. Add one 1 GB disk; the runtime host's data was about 4 MB
    after a local start. There is no model spend in the smoke checks. Any change of plan, such as moving the
    runtime host to Standard, needs Davide's word first.
```

## Owner's words (relayed verbatim)

On this release, Davide wrote to the implementer, in this session:

> "… if we are ready to test in production tell codex do deploy the latest branch on render and vercel …" (an excerpt; the rest of that message asked for the setup and observability checks reported in CC-0007)

and then:

> "Thank you. Please let's handoff the task to codex to create the 3 starter services. All the environment variables are in the free service. He just have to import them into the new services."

These words name deploying the latest branch on Render and Vercel, and three Starter services. They do not name the following, so please confirm them with Davide directly, as you did for OP-0004 revision 2:
- the migrations 0012–0014, the worker login and the runtime registration (E1–E3), without which the deploy cannot run;
- the project id and admin email;
- an expiry.

One correction to "just import them". The API service holds the LiveKit settings and the two model keys. The worker login and both capabilities do not exist yet, and E2–E4 create them. Each new service takes only the values listed for it below. Do not copy or link the API's whole environment: the API's database login, JWT settings and mail key would then sit in services that never use them.

## Ordered effects

Run everything from a checkout of the candidate commit, with Node 24.21.0 and pnpm 11.7.0 (`corepack prepare pnpm@11.7.0 --activate`, then `pnpm install --frozen-lockfile`). Generated values go only to the platform secret stores or the operator's private store, never to a transcript or this issue.

- **E1. Schema.**
  1. With the migration owner connection (session pooler, `verify-full`), run `SOPHIA_MIGRATION_DATABASE_URL=… pnpm db:migrate -- --dry-run`. Expect exactly 0012, 0013 and 0014 pending, with the checksums above.
  2. Then run `pnpm db:migrate`.
  3. Run the dry run again and expect 0 pending.

  The live `878aa9e6` API keeps working on this schema.
- **E2. Worker login.** As the owner, create a login role (e.g. `sophia_worker_app`) granted `sophia_worker`:
  - it has no SUPERUSER, BYPASSRLS, CREATEDB or CREATEROLE;
  - its pooler user is `sophia_worker_app.<ref>`;
  - its URL, with `verify-full` and the Supabase CA from `deploy/supabase/`, becomes `SOPHIA_WORKER_DATABASE_URL`.
- **E3. Runtime registration.** Run `node --env-file=<private env> scripts/register-runtime.ts <projectId> <adminEmail>`. It prints the runtime capability once; store it as `SOPHIA_RUNTIME_TOKEN`.
- **E4. Bridge capability.** Generate `openssl rand -hex 32` and store it as `SOPHIA_MEDIA_BRIDGE_TOKEN`. Its lowercase-hex SHA-256 (`printf %s "$TOKEN" | sha256sum`) becomes `SOPHIA_MEDIA_BRIDGE_TOKEN_SHA256`.
- **E5. API setting.** On `sophia-next-api`, add `SOPHIA_MEDIA_BRIDGE_TOKEN_SHA256` and save without deploying. Nothing else changes on the API.
- **E6. Deploy the API.**
  1. Change the service's branch from `studio/qol` to `claude/affectionate-cannon-496z9m`, keeping Auto-Deploy Off. Cancel any deploy this starts by itself, unless its commit is the candidate.
  2. Deploy exactly the candidate commit.
  3. Check S1 before going on.

  The instance stays free: the API alone used about 100 MB idle.
- **E7. Studio.** Build `apps/studio` at the candidate with the Production values of `VITE_API_URL`, `VITE_SUPABASE_URL`, `VITE_SUPABASE_PUBLISHABLE_KEY` and `VITE_AUTH_PROVIDERS`. Deploy it to production with the commit recorded.
- **E8–E10. Three Render background workers.** For each:
  - the same repository at the candidate commit;
  - the same region as the API;
  - the **Starter** plan, **one instance**, and Auto-Deploy **Off**.

  Build with `corepack enable && pnpm install --frozen-lockfile`. The runtime host's build adds more (see its row below). Node comes from `.node-version` (24.21.0).

  The API is on a free instance, outside Render's private network, so `SOPHIA_SERVICE_URL` is the API's **public https URL**. That is the same value as Studio's `VITE_API_URL`. The capability tokens guard those routes, and HTTPS carries them.

| Service | Start command | Copy from `sophia-next-api` (same name, same value) | New or literal values |
|---|---|---|---|
| **E8** `sophia-next-worker` | `node apps/worker/src/server.ts` | `LIVEKIT_URL`, `LIVEKIT_API_KEY`, `LIVEKIT_API_SECRET` | `SOPHIA_WORKER_DATABASE_URL` (E2) |
| **E9** `sophia-next-bridge` | `node apps/media-bridge/src/server.ts` | none under the same name. The API's Google key value is copied **under the name `GEMINI_API_KEY`**: the bridge reads only that name, and refuses to start without it | `SOPHIA_SERVICE_URL` (public API URL), `SOPHIA_MEDIA_BRIDGE_TOKEN` (E4), `SOPHIA_LIVE_MODEL=gemini-3.8-live`, `NODE_ENV=production`, `SOPHIA_BRIDGE_INSTANCE=render-bridge` |
| **E10** `sophia-next-runtime` | `node scripts/runtime-host.mjs --root /var/data/sophia/<projectId>` | `OPENAI_API_KEY` | `SOPHIA_SERVICE_URL` (public API URL), `SOPHIA_RUNTIME_TOKEN` (E3) |

The runtime host (E10) also:
- builds with `corepack enable && pnpm install --frozen-lockfile && pnpm build && pnpm artifacts`, and `pnpm artifacts` must reproduce the recorded identities;
- has a **1 GB persistent disk** mounted at `/var/data`;
- runs its model route, `openai/gpt-6-luna` at high reasoning, which has not yet run live.

## Smoke checks

None of these calls a paid model.

- **S1.** `GET /health` returns 200, and `GET /ready` returns 200 `{"ready":true}`. `/ready` covers migrations 0009–0014 and the role check.
- **S2.** A signed-in member loads the project in Studio:
  - no CORS errors;
  - the sign-in providers match `VITE_AUTH_PROVIDERS`;
  - the room page renders;
  - the runtime resource reads **online / idle**.
- **S3.** The worker logs `[sophia-worker] runtime dispatch started` and `room removal reconciliation started`.
- **S4.** The runtime host's events show it ready, bound to the API, with `live: true`, not rehearsal.
- **S5.** The bridge logs its start banner. The API's log shows `GET /v1/media/assignments` answered 200, not 401.
- **S6.** `apps/api/scripts/diagnose.ts` runs read-only. The full JSON goes to the private store, with a reviewed summary here.

## Watch during the test

- **Runtime host memory.** It used about 260 MB idle in rehearsal, including its dsh process, and a live model turn adds more. If Render restarts it for memory, report it. Moving that one service to Standard needs Davide's word.
- **The free API instance.** The bridge's and runtime host's polls keep it awake. It spends free instance hours while it runs.
- **After the test,** suspending the three services stops their compute; the disk is kept. The API service still holds the Google and OpenAI keys it never reads. Removing them is Davide's separate decision.

Codex follows `deploy/S1-05A-release.md` and reports per incident with the evidence contract (CC-0002).

## Not covered here (separate owner decisions)

- **Guests.** Anonymous sign-ins are off, so an invited guest cannot join.
- **LiveKit PII redaction.** It is off; turning it on before the live test is recommended.
- **`INVITE_FROM`.** Without it, invitation email is off; links and QR codes still work.
