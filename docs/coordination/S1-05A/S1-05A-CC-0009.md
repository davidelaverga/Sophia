# S1-05A-CC-0009: OP-0002's candidate moves to 0b9a8d0 (studio/qol sign-in providers); hosting of E8–E10 under revision

```yaml
protocol: sophia.dev-handoff.v1
message_id: S1-05A-CC-0009
from_role: implementer
reply_to: S1-05A-CC-0008
kind: clarification
operation_id: S1-05A-OP-0002
candidate_commit: 0b9a8d0e607a31ea6ae96d907487fe3ea6c60412
implementation_pr: https://github.com/davidelaverga/Sophia/pull/13
approval_ref: null                  # nothing here is an approval
summary: >
  Do not execute CC-0008 as posted. Its candidate becomes 0b9a8d0, and the hosting of E8–E10 is being revised
  with Davide. A revised request will follow once he decides.
```

- **The candidate moves to `0b9a8d0`.** `studio/qol` moved to `d18e171`, adding Studio sign-in with Google, GitHub and Microsoft (`33c5062`, `d18e171`). That code may already be in production Studio, so deploying `36cd1f2` could remove it. `0b9a8d0` merges it, with no conflicts.
  - The API, worker, bridge, runtime host, scripts and SQL are identical to `36cd1f2`.
  - The migration checksums are unchanged.
  - CI is green on all four jobs at `0b9a8d0`, in runs 36174908843 and 36174904141. Locally, `pnpm check` exits 0 with 223 unit and 56 integration tests.
- **E7, the Studio build, gains one Production value:** `VITE_AUTH_PROVIDERS`. It is the comma-separated providers enabled in Supabase Auth (e.g. `google,github`). When it is empty, only email sign-in shows. Take the value the current Production build uses.
- **CX-0010's bundle check** is now three-way:
  - `The same link creates your account` means the bundle was built from `b3da19e` or later;
  - a provider row (the `VITE_AUTH_PROVIDERS` code path) means `33c5062` or later;
  - `A project admin invites you by email` means earlier than `b3da19e`.
- **E8–E10 are on hold.** Davide is choosing where the worker, media bridge and runtime host run. The options are his own computer or paid Render background workers, with the API staying on its free instance. I measured the idle memory locally, in rehearsal mode:
  - worker about 60 MB;
  - bridge about 70 MB;
  - runtime host with its dsh process about 260 MB;
  - API about 100 MB.

  All four do not fit the free instance's 512 MB, so they cannot share it. If the API stays free, the three processes reach it at its public URL: the code does not require the private network, and the capability tokens guard the routes. The revised request will name the plan or host, and `SOPHIA_SERVICE_URL`, for each.
