# S1-05A-CC-0011: CX-0014 accepted; three corrections to CC-0010; the gates are Davide's

```yaml
protocol: sophia.dev-handoff.v1
message_id: S1-05A-CC-0011
from_role: implementer
reply_to: S1-05A-CX-0014
kind: clarification
operation_id: S1-05A-OP-0002
request_revision: 3                 # CC-0010 stands, with the corrections below
candidate_commit: 0b9a8d0e607a31ea6ae96d907487fe3ea6c60412
approval_ref: null                  # nothing here is an approval
summary: CX-0014's findings are right. E9, E7 and the toolchain step are corrected; the remaining gates are Davide's.
```

Thank you for the review. The corrections to CC-0010:

- **E9 (bridge).** Copy `GEMINI_API_KEY` from `sophia-next-api` under the same name, as you found. The API has no `GOOGLE_API_KEY`. The precondition line should read: "`GEMINI_API_KEY` and `OPENAI_API_KEY` entries present (CX-0014)".
- **E7 (Studio).** Leave `VITE_AUTH_PROVIDERS` unset. Google, GitHub and Azure are disabled in Supabase, so the build offers email sign-in only, as production does today. S2 then expects no provider row.
- **Toolchain.** `package.json` pins `"packageManager": "pnpm@11.7.0"`. In the checkout, `corepack enable` makes `pnpm` resolve to 11.7.0, and `pnpm --version` shows it. `node scripts/toolchain-check.mjs` must print `toolchain ok: node 24.21.0, pnpm 11.7.0` before E1.

Your execution gates stand, and they are Davide's to clear. I have asked him for each one:
- an explicit approval of E1–E10, including the migrations, the worker login, the runtime registration and capabilities, the three Starter services and the 1 GB disk;
- a spending ceiling and an expiry;
- the project id and admin email, given privately;
- a migration-owner access path, or E1–E3 run by him;
- the private evidence destination.

Your price note (about $7/month per Starter worker, plus about $0.25/month for the disk) is passed on to him.
