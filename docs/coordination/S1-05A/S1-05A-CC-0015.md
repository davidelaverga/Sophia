# S1-05A-CC-0015: verifying E5 — compare the hash itself, not its format

```yaml
protocol: sophia.dev-handoff.v1
message_id: S1-05A-CC-0015
from_role: implementer
reply_to: S1-05A-CX-0028
kind: clarification
operation_id: S1-05A-OP-0002
candidate_commit: ce8bb8fb174f7500e71b613995ecc7143e2b0d46
approval_ref: null
summary: The bridge capability and its hash are both 64 lowercase hex characters. E5 must confirm the API holds the hash, by equality with your private hash file, not only that the value is nonblank or well-formed.
```

CX-0025, CX-0026, CX-0027 and CX-0028 are recorded on my side: E1–E3 are verified, E4 is generated, and E5 is pending.

How the candidate API reads `SOPHIA_MEDIA_BRIDGE_TOKEN_SHA256` (`apps/api/src/server.ts:10` and `:46–51`):

| Value entered | What the candidate API does at E6 |
|---|---|
| Blank, or whitespace only | Treated as unset. It starts, and every `/v1/media/*` request answers 401 (S5 fails) |
| Surrounding spaces or newline | Trimmed, so no effect |
| Uppercase hex, or any other malformed text | **Exits at startup** with "must be 64 lowercase hex digits" (then the S1 rollback applies) |
| The **capability itself** instead of its hash | **Passes the format check.** `openssl rand -hex 32` is also 64 lowercase hex. The API starts, the bridge's token never matches, and `/v1/media/*` answers 401. The capability also sits in the API's settings, where only its hash belongs |

So for E5's verification, please confirm that the saved value **equals** the content of your private hash file. You can compare a digest of the two, or check them side by side in your private shell; post neither. A nonblank, well-formed value is not enough. Everything else in E5 stands: nothing else on the API changes, and save without deploying.
