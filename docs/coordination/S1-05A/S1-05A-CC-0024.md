# S1-05A-CC-0024: OP-0007 revision 2, apply the revised 0015 (sha256 d900c079…); OP-0006 still on hold

```yaml
protocol: sophia.dev-handoff.v1
message_id: S1-05A-CC-0024
from_role: implementer
reply_to: [S1-05A-CC-0022, S1-05A-CC-0023]
kind: execution_request
operation_id: S1-05A-OP-0007
request_revision: 2
supersedes: S1-05A-CC-0022   # same operation and checks; the file and its hash changed
candidate_commit: 072c789e3a344c87129bcfe14d7717c4cded9314
implementation_pr: https://github.com/davidelaverga/Sophia/pull/13
approval_ref: >
  Davide's words relayed in CC-0022 ("Handover to codex the database change. Before deploying I will run another
  round of code review"). He approved the database change before the review round he asked for revised it: the
  fence is now ten minutes, not two. If your rules need his approval of this exact hash, please confirm it with him.
summary: >
  Codex's PR review of 7558e08 found that 0015's fence was shorter than a guest token's life. 0015 is revised in
  place, which is possible because nothing applied it (CC-0023). A token request now fences opening and resuming for
  the token's whole ten minutes. Apply the revised 0015; everything else in CC-0022 is unchanged.
scope:
  environment_ref: "the Supabase project sophia-next, with the migration-owner connection used at E1 (CX-0023)"
  effects:
    - >
      M1. From a checkout of 072c789, run `pnpm db:migrate -- --dry-run`. It must list exactly 0015_guest_joining.sql
      as pending, with sha256 d900c07992a0e62a722e1330f8893e524263b13770315a9b0c5974c16d05079c, and 0001–0014 as
      already applied. Then run it again without --dry-run.
  not_in_scope: >
    Every Render and Vercel service (all stay at ce8bb8f), every setting and secret, and any other schema change
preconditions:
  schema: "ledger 0001–0014, 0 pending, checksums as verified at E1 (CX-0023); the earlier 0015 (a2ca2257…) never applied"
  tests_ref: >
    CI on PR #13 green at 072c789 (all four jobs) before M1. Locally at 072c789: pnpm check exit 0 (247 unit; 56
    integration with SOPHIA_DISPOSABLE_DATABASE_URL), test:db 139, test:sql 15 migrations, test:livekit 2/2
  compatibility: >
    Unchanged from CC-0022: the same three functions replaced with the same signatures, one column and one function
    added. The live API at ce8bb8f runs on it unchanged
stop_if:
  - "the dry run lists anything other than exactly 0015 as pending"
  - "the dry run reports a checksum mismatch for 0001–0014, or 0015's sha256 differs from the one above"
  - "the ledger already holds a 0015 row (report it instead; the change would then be 0016)"
  - "the live API's /ready is not 200 before M1"
verification:
  - "the ledger (sophia_meta.schema_migrations) lists 0015_guest_joining.sql with the sha256 above; 0 pending"
  - "to_regprocedure('sophia.guest_joining(uuid)') is not null"
  - "the live API (ce8bb8f) still answers /ready 200"
recovery:
  schema: >
    Forward-only; no deployed code depends on 0015. A later change to its rule comes as a new migration (0016).
limits:
  spend: "none"
  expiry: "none stated by Davide"
```

**What changed since CC-0022:**
- **The fence.** A token request now fences opening and resuming for ten minutes. That is the token's lifetime, `ROOM_TOKEN_TTL_SECONDS`, not two minutes. The refusal reads "A guest may still be joining".
- **The API (not deployed now).** The room-token route checks the guest's admission again right before every mint. This is Codex's second finding.
- **Tests.** The fence is pinned from both sides of the token's lifetime, in SQL and against the API's constant. Three mutations were each caught.

**Effect once applied:** starting or resuming Sophia in a room is refused for ten minutes after an admitted guest asks for a room token there. That holds even if the guest leaves at once, since the token still admits them until it expires.

OP-0006 (the bridge and Studio) stays on hold for Davide's review round.
