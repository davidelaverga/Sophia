# S1-05A-CC-0030: OP-0008, observe Davide's production test and report it (read-only)

```yaml
protocol: sophia.dev-handoff.v1
message_id: S1-05A-CC-0030
from_role: implementer
reply_to: [S1-05A-CC-0029]
kind: inspect_request
operation_id: S1-05A-OP-0008
candidate_commit: 2d59884b6cf87e58a78a4b171072cf04ce88a43f
implementation_pr: https://github.com/davidelaverga/Sophia/pull/13
approval_ref: >
  Davide's words in CC-0029: "Make sure to handover to codex exactly what he needs to monitor and observe for a
  proper later report after testing in production". Read-only throughout.
summary: >
  Once OP-0006 revision 5 is verified, capture a baseline, watch Davide's production test, then report per test step
  with the log evidence. This changes nothing: no deploy, setting, secret, restart, rollback or database write.
scope:
  sources:
    - "Render logs for sophia-next-bridge, sophia-next-api, sophia-next-worker and sophia-next-runtime, captured within the window (Render keeps them only for a limited time)"
    - "apps/api/scripts/diagnose.ts at 2d59884: a read-only snapshot, with SOPHIA_DIAGNOSE_DATABASE_URL set to the owner connection and the LiveKit settings for the room listing"
    - "The LiveKit Cloud room trace for the test room, if your access shows it"
    - "Davide's own account of what he saw and heard at each step (ask him when the test ends)"
  not_in_scope: "any effect. If something breaks, tell Davide at once and name CC-0029's rollback; roll back only on his word"
window: "from OP-0006's verification until Davide says the test is over, at most 24 hours"
limits:
  spend: "none (Davide's test uses the models; this observation does not)"
privacy: >
  #14 gets a reviewed summary only. No transcripts, audio, brief text, tokens, keys, emails, names or participant
  identities. Ids appear as 8 characters. LiveKit's PII redaction is off, so its trace goes in as counts only. Raw
  logs and the diagnostic's full JSON go in the owner's private store, referenced from the report
```

## 1. Before the test

1. **The release manifest.** For all five processes: the deploy id, source commit and state. Every one should read `2d59884`. Add the ledger (0001–0016, 0 pending) and `/health` and `/ready` 200.
2. **The baseline diagnostic.** Run `node apps/api/scripts/diagnose.ts --project <the test project>` once. Its summary counts are what the test is compared against.
3. **Ready signals.** In the logs:
   - the bridge's `bridge.start` banner, and no `assignments.failed` since;
   - the worker's `[sophia-worker] runtime dispatch started`;
   - the runtime host's ready event with `live: true`.

   The Studio's runtime resource should read online.
4. **Timekeeping.** Note the UTC time Davide starts. Ask him to say when each step begins, or to note the times himself.

## 2. The test, step by step

Davide does each step. Codex records what the logs show. Bridge lines are JSON `{"at","event",…}` with `exchangeId`. Take the `exchangeId` from `session.start` and filter by it. T1–T4 and T9 need only Davide. T6 and T7 need a second person signed in with their own account who is not a member of the project. Anonymous sign-ins are off, so a signed-out guest sees "Guest access is not turned on".

| Step | Davide does | He should see or hear | The logs should show |
|---|---|---|---|
| **T1 Join** | Joins the room, then starts Sophia | She joins; the line reads "Sophia is listening to Davide". **No pause at join** | Bridge: `session.start`, `room.joined` (`people` ≥ 1), `provider.ready`, `assignment.changed`. No `holder.absent` in the first 10 s (CX-0044). API: the exchange start 2xx |
| **T2 Long reply** | Asks for a long answer (1–2 minutes), with headphones | The whole reply, no skipped or jumped words | One `audio.reply` with `ended: played`, `droppedMs: 0`, `playedMs` ≥ `receivedMs`. Record `turns`, `receivedMs`, `arrivalMs`, `playedMs`, `maxQueuedMs` (CX-0045/0046) |
| **T3 Talk over her** | Speaks over a long reply | She stops within about a second of his words, then answers them | `audio.reply` with `ended: interrupted` and `clearedMs` > 0. Known limit: if Google starts its answer before its transcript of his words arrives, the rest of the old reply plays first |
| **T4 Stop Speaking** | Presses Stop Speaking mid-reply | The reply stops at once; the line reads "listening" at once; the stopped reply never comes back | `audio.reply` with `ended: stopped`; `assignment.changed` with the playback epoch moved; zero or more `audio.reply_fenced` for the stopped turn, and no audio from that turn afterwards |
| **T5 A brief by voice** | Asks Sophia to draft a brief | The work view shows the task queued, then running, then its result | Bridge `tool.answered` with `name: start_brief`, `status` ok and a `workId`. Worker `dispatch outbox …: enqueued as runtime command … (seq n)`. Runtime host events handling that command. Diagnostic: the native task's phase, and the command's receipt stages in order |
| **T5b (optional) Hold and Resume** | Holds the goal while the brief runs, then resumes it | Report what the work view shows | No result from a turn that ran before the Hold is published after the Resume (0016). Diagnostic: the task's phase and reason, and the commands' authority epochs as the diagnostic shows them |
| **T6 Guest fence** | Sophia live. The guest knocks and Davide lets them in. The guest leaves at once, and Davide presses Resume straight away | Sophia pauses before the guest's call connects. Resume is **refused** with "A guest may still be joining: Sophia resumes when the room is member-only again", for up to 10.5 minutes after the guest's last token. After that, Resume works | API: the guest's room-token request 2xx. Bridge: the pause (`assignment.changed`), and no `quiesce.ack_failed`. API: `request refused` with 409 `invalid_state` on the Resume, and a later Resume 2xx. Record the times: refusals should stop about 630 s after the token (0015 and the API at `2d59884`) |
| **T7 (optional) Remove a guest** | Declines or removes an admitted guest who is in the call | The guest is out of the call within seconds | Worker `room removal <id> removed`, never `still pending` for long. LiveKit: the guest's participant gone (as a count). Diagnostic: the removal row settled |
| **T8 Leave during a join** | Clicks Join, then Leave within a second, or closes the tab while it reads "Joining…". Then joins normally | The browser's microphone indicator goes off. Nobody is left in the room behind the "left" screen | LiveKit: no participant of Davide's remains after the Leave (counts over time). If Sophia was live: `holder.absent` and a pause, which are correct here (Studio `08ce24f`) |
| **T9 End** | Ends the conversation | Sophia leaves; the room reads ended | Bridge `session.close` with `lost: false`, and `audio.reply` with `ended: closed` if she was mid-reply. Diagnostic: the exchange ended |

## 3. Watch throughout, and report every occurrence with its UTC time and `exchangeId`

- **Bridge**:
  - reconnects and losses: `provider.recover` (with Google's reason), `session.lost`, `session.unavailable`, `session.start_failed`, `room.connection` (with LiveKit's reason);
  - `holder.absent` (with `departed` and `people`) outside T8;
  - tool problems: `tool.dropped`, `tool.failed`, and `tool.answered` with a refusal `status` (for example after a turn ended);
  - announcements: `announce.not_heard`, `announce.record_failed`;
  - reports: `quiesce.ack_failed`, `presence.report_failed`, `holder.event_failed`, `room.attributes_failed`, `room.close_failed`, `assignments.failed`;
  - audio: `audio.refused`, `audio.output_refused`, `audio.playback_failed`, and any `audio.reply` with `droppedMs` > 0, or with `ended: played` and `playedMs` below `receivedMs`;
  - restarts and crashes.
- **API**: every 5xx with its cause (no values); `request refused` counted by status, code and route; requests slower than 5 s; free-instance cold starts.
- **Worker**: `denied` or `outcome_unknown` dispatches; `deferred` lasting over a minute; `room removal … still pending`.
- **Runtime host**: restarts, lost readiness, errors, and commands without an outcome.

## 4. After the test

1. **Capture the logs.** Save the window's logs from all four Render services before they age out, and store them privately.
2. **The after-test diagnostic.** Run it once more and compare it with the baseline.
3. **Davide's account.** Ask him, step by step, what he saw and heard, and any error text shown in the Studio.
4. **Post a `result` for OP-0008 on #14,** under 80 lines, with:
   - the window, and the release manifest (five deploy ids and commits, and the ledger);
   - **a per-step table:** T1–T9, with what Davide saw, what the logs show, and a verdict (as expected / not as expected / not run);
   - **an `audio.reply` table:** one row per reply, figures only (`ended`, `turns`, `receivedMs`, `arrivalMs`, `playedMs`, `droppedMs`, `clearedMs`, `maxQueuedMs`);
   - **every item from §3,** or "none";
   - the API refusals by status and code; the worker's dispatch outcomes; the runtime host's restarts; the summed `provider.usage` `totalTokens`;
   - the diagnostic before and after, as a reviewed summary;
   - the reference to the private evidence; what is unresolved; and one next action.

Anything unexpected is reported as observed, with its evidence and no fix. The implementer turns it into findings and code.

---
_Generated by [Claude Code](https://claude.ai/code)_
